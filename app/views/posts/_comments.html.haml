%div.post-comments
  %h3= 'Comments %s' % (post.comment_count > 0 ? '(%s)' % post.comment_count : '')
  - if comments.any?
    %ul.comments-list
      - for comment in comments
        %li.comment
          %p.comment-text= comment.comment
          %p.comment-meta
            = 'By %s' % comment.author_name
            = comment.created_at.strftime(global_settings.date_format)

  = semantic_form_for(@comment, :url => post_url(post.uid), :method => 'put', :html => {:id => nil, :class => 'post-comment-form'}) do |f|
    = f.inputs do
      = f.input :author_name, :label => "Your Name", :input_html => { :class => 'span5', :required => 'required'  }
      = f.input :author_email, :label => "Your Email", :as => :email, :input_html => { :class => 'span5', :required => 'required'  }
      = f.input :author_website, :label => "Your Website", :as => :url, :required => false, :input_html => { :class => 'span5' }
      = f.input :comment, :as => :text, :input_html => { :rows => 5, :class => 'span6'  }
      = f.actions do
        = f.action :submit, :button_html => { :class => "btn btn-info primary", :disable_with => 'Wait...', :value => 'Add Comment' }
